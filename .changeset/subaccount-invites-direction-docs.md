---
"@polyester/sdk": patch
---

Clarify `subaccounts.listInvites` docs: omit `direction` to list both incoming and outgoing invitations, or pass `"incoming"` or `"outgoing"` to filter. `"all"` is not an accepted value.
